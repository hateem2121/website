# DECISIONS.md — RUN APPAREL Website Build

Append-only record of Hateem's approvals and sign-offs (format per CLAUDE.md §4: date · decision · one-line context). **Never rewrite or delete an entry.** If planned work would contradict a recorded decision, stop and ask.

---

- 2026-07-12 · Constitution v1 filed at repo root (`CLAUDE.md`, Build Master Prompt v1.1, Company Master Prompt, RFQ Plan v2.0-reconstructed) · Per Hateem's Session-1 instruction; documents are read-only to Claude (spec §0.1, CLAUDE.md §2).
- 2026-07-12 · Phase 0 execution plan approved · Session 1 plan (constitution filed, memory files, chunked Phase 0 stages A–G through the spec §17 row-0 gate); execution may begin.
- 2026-07-12 · CF1 — Cloudflare Email Routing NOT used · Hostinger already runs catch-all on both wear-run.com and wear-run.help, forwarding all mail (incl. `partner@`) to `hateem@wear-run.com`; enabling Cloudflare Email Routing would seize MX and break it. Chunk E5 dropped; SPF-merge safeguard retained but now applies between Hostinger's and Resend's SPF on wear-run.com.
- 2026-07-12 · Cloudflare access = scoped API token · Claude runs the D1/R2/DNS automation via a revocable API token Hateem adds to this environment (`CLOUDFLARE_API_TOKEN` + `CLOUDFLARE_ACCOUNT_ID`).
- 2026-07-12 · Deploy mechanism = Cloudflare Workers Builds · Build/deploy on git push via Cloudflare's Git integration; GitHub Actions reserved for the nightly backup (spec §3.10 pick-one).
- 2026-07-12 · Cloudflare Access on `/admin` = ENABLED · Free Zero Trust login wall in front of the CMS admin (spec §3.8, optional-but-recommended).
- 2026-07-13 · Readiness confirmed · Workers Paid ACTIVE, card on file, Hostinger admin — all yes. DNS locations: **wear-run.com on Hostinger, wear-run.help on Cloudflare** (reverse of spec §3.5's assumption; reconcile the `assets.` domain later).
- 2026-07-13 · Cloudflare access via the **Cloudflare Developer Platform connector** (OAuth through Claude), superseding the env-var API-token mechanism from the 2026-07-12 access decision · Hateem connected it and authorized its use; it lets Claude manage D1/R2/Workers securely with no credential in the visible environment box (CLAUDE.md §11, spec §3.7). DNS, Worker deploy, secrets, and protections remain dashboard actions (the connector does not expose them).
- 2026-07-13 · **Single branch `main` only** (Hateem's explicit instruction; matches CLAUDE.md §8) · All Phase 0 work consolidated onto `main` (PR #1 merged); the session feature branch is retired and future work commits directly to `main`. This overrides the session's default feature-branch + PR workflow for this repo. (Stale remote branch `claude/phase-0-kickoff-ou58xh` couldn't be deleted from here — the git proxy blocks ref deletion — so it awaits a one-click delete in GitHub.)
- 2026-07-15 · Working model switched to **local-first on Hateem's Mac** · The Mac folder `~/Sites/website-main` was verified byte-identical to GitHub `main` and re-attached with full git history; work now happens locally and pushes to `main` (which auto-deploys via Workers Builds). Rule: pull before starting, push at end of work block.
- 2026-07-15 · Production smoke-test writes authorized · Hateem explicitly authorized insert/update/delete of smoke-test rows in the production `users` and `media` tables of `run-apparel-db` (§15 requirement), and logged into `/admin` himself for the Payload-path test.
- 2026-07-15 · **PHASE 0 SIGNED OFF** (spec §17 row 0, option A) · All three gate items verified green: deployed starter reachable on workers.dev; §15 smoke test green for Phase-0 scope (SQL-layer CRUD with delete-persistence on `users`+`media`, full Payload-path media upload→R2→delete→verified-gone, admin login); first nightly backup file verified in R2 (`backups/run-apparel-db-2026-07-15.sql`). Remaining dashboard protection/billing items stay on the open list (runbook §5) and do not block Phase 1.
- 2026-07-15 · Phase-1 direction decisions (during "Paper & Ink" synthesis) · Motion level = **Balanced** (signature moments kept; whole-page skew + heavy blur dropped) · About-page map = **3D globe** reusing the spec §7 Three.js engine (no new dependency) · Display font: Hateem holds Neue Stance files and wants it self-hosted — **commercial web licence unconfirmed** (PP fonts are try-only for free); build proceeds on free Archivo + Instrument Serif with Stance as a licensed swap-in later (open item).
- 2026-07-16 · Palette variant **A · Olive Ink** chosen · Picked from three rendered boards (Olive Ink / Carbon / Pine); volt `#CDF345` constant; warm lime-cast dark surfaces.
- 2026-07-16 · **Spec §6 palette-rule override — volt lime** · §6 rules out the neon-green sports cliché; after first-hand study of Hateem's four references (landonorris.com, wembi.ai, hildenkaira.fi, sharebien.com — all lime-signal sites) Hateem deliberately chose the volt-lime accent family. Framing: volt is a *signal* inside a paper/ink editorial system, never the whole identity; a §6 amendment wording is proposed and awaits Hateem's approval (spec files are read-only to Claude).
- 2026-07-16 · **PHASE 1 SIGNED OFF** (spec §17 row 1, option A: "Lock it") · Direction **"PAPER & INK · Olive Ink"** picked; tokens locked in `docs/design/DESIGN.md` (colors light+dark with AA verification, type system, motifs, motion rules, component notes). Preview: `docs/design/directions/direction-d-paper-ink.html`.
- 2026-07-16 · **Spec §6 amended — "no corporate blues/greens" prohibition REMOVED (supersedes the entry above)** · Offered Hateem a choice: scope a narrow exception for volt lime, or remove the ban entirely. He chose full removal — a standing change to the palette rule for all future design decisions, not limited to the current volt accent. Applied verbatim to `RUN_APPAREL_Website_Build_Master_Prompt.md` §6, point 2: the prohibition sentence is deleted, with an inline note marking the amendment and pointing here.
- 2026-07-17 · **CLAUDE.md amended — "## gstack (recommended)" section added** (first amendment to this file; applied under the §2 process) · Hateem asked for gstack team mode in the project. He originally specified `gstack-team-init required`, which appends a "REQUIRED" section *and* installs a `.claude/hooks/check-gstack.sh` PreToolUse hook returning `permissionDecision: deny`. On being shown the exact text he **chose `optional` instead** — the recommended section, **no enforcement hook**. Reasons surfaced before he chose: (1) the REQUIRED text's "before doing ANY work, verify gstack" competes with §3's session ritual; (2) its "do not ignore gstack errors or work around missing gstack" contradicted the live state, `/browse` being broken at that moment; (3) the deny hook makes a directory *outside* the repo a single point of failure for all work *inside* it; (4) §1 records Hateem as sole operator, so the teammate rationale did not apply. Applied exactly as approved; the section was moved above the version footer so the footer still closes the document. `gstack` global `team_mode: true`.
- 2026-07-17 · **Phase 2 migration authorized and APPLIED to production D1** (option A: "rename, keep my account") · Hateem was shown the exact SQL touching his account — 3 statements out of 329 — and approved. Delivered via `Admins.dbName = 'users'` rather than hand-written table renames: the collection is `admins` per spec §4.1 while the physical table stays `users`, so the account row is never moved. Verified after applying: account intact with `role=admin`, password and `created_at` untouched, 81 tables, 5 categories seeded, live `/admin` still serving. Two generated-migration defects (NOT NULL without default; `role` defaulting to `editor` and silently demoting him) and one data-corruption bug (SQLite double-quote misfeature writing `'buyers_id'` into an FK column of his 2 saved-preference rows) were found and fixed before applying.
- 2026-07-17 · **gstack upgraded v1.26.3.0 → v1.60.1.0 (global, `~/.claude/skills/gstack`)** · Was already installed but 100 commits stale; `auto_upgrade`/`update_check` were already `true`. The upgrade **broke `/browse`** (new Playwright wants a browser build the cache lacked) and four install attempts failed — Playwright's downloader has a 30s timeout and no resume, and Hateem's link stalls, so a 162 MB single-shot transfer never completed. Fixed by fetching both Chrome-for-Testing zips with `curl -C -` (resumable) and installing them into the Playwright cache with the marker files it expects. `/browse` re-verified end-to-end against a live page (HTTP 200, text extracted). Global `~/.claude/CLAUDE.md` skill list refreshed to the real 53 skills. No project code touched.
- 2026-07-17 · **New-major upgrades: Hateem approved all 7; only 2 were actually installable** · He chose "all seven, latest everything" (§0.4 requires the ask; his call). On testing, 5 are genuinely incompatible and were reverted — this is evidence, not caution: **graphql 17** breaks `payload@3.86`'s declared peer `^16.8.1` · **typescript 7** violates typescript-eslint's peer `>=4.8.4 <6.1.0` and **crashes `pnpm lint`** (`Cannot read properties of undefined (reading 'Cjs')`) — exactly what the Phase-0 log predicted ("pin the mature line the whole ecosystem targets") · **eslint 10** exceeds `eslint-config-next`'s `^9` cap · **@vitejs/plugin-react 6** needs `vite@^8` (vitest ships 7) · **@types/node 26** would describe Node 26 APIs while the machine runs Node 24, so the types would lie. Applied: **jsdom 29.1.1, dotenv 17.4.2**. Unmet peers went 14 → 1 (the remaining one, `typescript@^5.0.0` vs 6.0.3, predates this work). Re-ask when the ecosystem catches up — TypeScript 7 is the one worth revisiting.
- 2026-07-17 · **Node upgraded 24.14.1 → 24.18.0 on Hateem's Mac (latest LTS)** · He challenged the claim that Node 24 was anomalous and was right: nodejs.org lists 24.18.0 as "Latest LTS" (26 is Current, not LTS). The stale pin was the project's `.node-version: 22`; the Payload template wants ≥24.15 and Phase 0 had lowered `engines.node` to >=20.9.0 to fit a Node-22 container. Node was subsequently proven NOT to be the cause of the local build failure (22.23.1, 24.14.1 and 24.18.0 all fail identically). `.node-version` left at 22 pending the Phase-3 investigation, since changing it buys no fix and would move CI off a green config.
- 2026-07-17 · **Local-build fixes approved — option A, apply both** (Session-5 investigation; evidence in BUILD_LOG) · (1) `payload.config.ts` hands inert stub bindings to the build phase (`NEXT_PHASE === 'phase-production-build'`) instead of booting one Miniflare per build worker — a throw-on-access probe proved nothing reads D1/R2 during builds; kills the SQLITE_BUSY class at any core count, local and CI. (2) `package.json` `build` script pins `NODE_ENV=production` via cross-env, guarding against the Claude Code app's environment leak (`NODE_ENV=development`) that caused the useContext prerender crash. Both validated end-to-end with the exact CI command before this approval; runtime, CLI/migrations, and `next dev` paths unchanged.
- 2026-07-17 · **Dependency re-audit applied + Node synced to 24 everywhere** (option A) · Re-audit vs the live npm registry found 28/29 deps already newest-stable inside their locked majors; Hateem approved the two drifted pins: `wrangler` 4.111.0 → 4.112.0 and `packageManager` pnpm 10.33.0 → 10.34.5. Deferred by recommendation: pnpm 11, and `@vitejs/plugin-react` 6 + vite 8 (no longer blocked — vite 8 is stable and vitest permits it — but it only serves React component tests, of which there are none). Still hard-blocked: graphql 17 (payload peer `^16.8.1`), typescript 7 (typescript-eslint peer `<6.1.0`), eslint 10 (capped by `eslint-plugin-import`/`jsx-a11y`/`react` at `^9`, NOT by `eslint-config-next`, which allows `>=9.0.0` — the 2026-07-17 majors entry's stated reason was wrong; verdict unchanged). Node 26 declined on evidence: it enters LTS 2026-10-28 per `nodejs/Release`, so it is "Current" today; revisit then. **Node sync (Hateem's catch — "shouldn't everything be in sync?"):** `.node-version` 22 → **24.18.0**, `.github/workflows/backup.yml` 22 → **24.18.0**, `engines.node` >=20.9.0 → **>=24.15.0** (matching the official with-cloudflare-d1 template), aligning CI, the nightly backup, the Mac, and `@types/node` on one version. The old "hold at 22 pending the Phase-3 investigation" reason expired when Session 5 cleared Node as a cause. Full pipeline re-verified green after both changes; the untestable-locally residual is that Cloudflare CI has never built on Node 24 (a failed build cannot replace the running Worker, so the live site is safe either way).
- 2026-07-19 · **About / Heritage page approved and merged to `main`** (PR #4, `067bab4`/`fd2ffe8`) · Hateem reviewed the deployed preview and said "merge." Page delivers spec §5's About/Heritage requirements with the installed stack (Motion reveals, not yet GSAP/3D globe — flagged as a separate future ask, not silently substituted). Founder framing (Ghafuree, 1889, never "Sandal"), certification-ecosystem framing, and JSON-LD `foundingDate: 2020` all verified in the rendered page before merge. This is a copy-approval decision under Phase 3's page-by-page gate (spec §17), distinct from and in addition to the code merge itself.
